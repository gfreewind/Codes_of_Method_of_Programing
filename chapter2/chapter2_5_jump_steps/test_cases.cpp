#include <iostream>
#include <limits>
#include <algorithm>
#include <gtest/gtest.h>

#include "jump_steps.hpp"

int main(int argc, char **argv) 
{
	jump_steps(1);
	jump_steps(2);
	jump_steps(3);
	jump_steps(4);
	jump_steps(5);
	jump_steps(6);
	
	return 0;
}


